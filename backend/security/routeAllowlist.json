[
  {
    "method": "GET",
    "path": "/health",
    "exposure": "public",
    "notes": "Infrastructure healthcheck, deliberately outside /api."
  },
  {
    "method": "GET",
    "path": "/api/chat/:id",
    "exposure": "public",
    "notes": "Anonymous by design (ADR 0002). No list-all-chats route exists -- there is no ownership model to list against. :id is a per-conversation high-entropy token (see backend/models/ChatModel.js), not the Mongo ObjectId, so a transcript cannot be reached by guessing/enumerating ids."
  },
  {
    "method": "POST",
    "path": "/api/chat",
    "exposure": "public",
    "notes": "Anonymous by design (ADR 0002). Protected instead by the abuse guard: body size cap -> per-IP rate limit -> daily token ceiling, all before any Anthropic call. See fitness function f1."
  },
  {
    "method": "DELETE",
    "path": "/api/chat/:id",
    "exposure": "public",
    "notes": "Anonymous by design (ADR 0002). Not billable, so outside the abuse guard's scope."
  },
  {
    "method": "PATCH",
    "path": "/api/chat/:id",
    "exposure": "public",
    "notes": "Anonymous by design (ADR 0002). Protected instead by the abuse guard, same as POST /api/chat. See fitness function f1."
  }
]
